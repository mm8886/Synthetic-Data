with col4:
    # Calculate channel distribution counts
    channel_counts = batch_results['1_Preferred_Channel'].value_counts()
    
    # Create a formatted string showing counts for each channel
    channel_info = "**Channel Distribution:**\n\n"
    for channel, count in channel_counts.items():
        percentage = (count / len(batch_results)) * 100
        channel_info += f"• **{channel}**: {count:,} ({percentage:.1f}%)\n"
    
    # Display the channel distribution information
    st.metric(
        "Channel Distribution", 
        f"{len(batch_results):,} Total",
        help=channel_info
    )
    
    # Show detailed breakdown on expander
    with st.expander("View Channel Breakdown"):
        st.write(channel_info)